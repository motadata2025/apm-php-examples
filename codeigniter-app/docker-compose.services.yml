version: '3.8'

# Codeigniter App Application - Supporting Services
# Provides MySQL, PostgreSQL, and Redis for Codeigniter App application

services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: codeigniter_app-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: codeigniter_app_db
      MYSQL_USER: codeigniter_app_user
      MYSQL_PASSWORD: codeigniter_app_password
    ports:
      - "3310:3306"
    volumes:
      - codeigniter_app_mysql_data:/var/lib/mysql
    networks:
      - codeigniter_app-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # PostgreSQL Database
  postgres:
    image: postgres:15
    container_name: codeigniter_app-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: codeigniter_app_db
      POSTGRES_USER: codeigniter_app_user
      POSTGRES_PASSWORD: codeigniter_app_password
    ports:
      - "5436:5432"
    volumes:
      - codeigniter_app_postgres_data:/var/lib/postgresql/data
    networks:
      - codeigniter_app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U codeigniter_app_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: codeigniter_app-redis
    restart: unless-stopped
    ports:
      - "6383:6379"
    volumes:
      - codeigniter_app_redis_data:/data
    networks:
      - codeigniter_app-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  # phpMyAdmin for MySQL Management
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: codeigniter_app-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: rootpassword
    ports:
      - "8089:80"
    depends_on:
      - mysql
    networks:
      - codeigniter_app-network

  # pgAdmin for PostgreSQL Management (FIXED)
  pgadmin:
    image: dpage/pgadmin4
    container_name: codeigniter_app-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin123
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
      PGADMIN_LISTEN_PORT: 80
      PGADMIN_LISTEN_ADDRESS: '0.0.0.0'
    ports:
      - "8090:80"
    volumes:
      - codeigniter_app_pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - codeigniter_app-network

  # Redis Commander for Redis Management
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: codeigniter_app-redis-commander
    restart: unless-stopped
    environment:
      REDIS_HOSTS: local:redis:6379
    ports:
      - "8091:8081"
    depends_on:
      - redis
    networks:
      - codeigniter_app-network

volumes:
  codeigniter_app_mysql_data:
    driver: local
  codeigniter_app_postgres_data:
    driver: local
  codeigniter_app_redis_data:
    driver: local
  codeigniter_app_pgadmin_data:
    driver: local

networks:
  codeigniter_app-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.24.0.0/16
