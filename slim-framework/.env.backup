APP_ENV=production
APP_DEBUG=false

# Database Configuration (Docker services) - ISOLATED
DB_HOST=127.0.0.1
DB_PORT=3309
DB_DATABASE=slim_framework_db
DB_USERNAME=root
DB_PASSWORD=rootpassword

# MySQL Configuration (Docker services) - ISOLATED
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3309
MYSQL_DATABASE=slim_framework_db
MYSQL_USERNAME=root
MYSQL_PASSWORD=rootpassword

# PostgreSQL Configuration (Docker services) - ISOLATED
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5435
POSTGRES_DATABASE=slim_framework_db
POSTGRES_USERNAME=slim_framework_user
POSTGRES_PASSWORD=slim_framework_password

# Redis Configuration (Docker services) - ISOLATED
REDIS_HOST=127.0.0.1
REDIS_PORT=6382
REDIS_PASSWORD=
